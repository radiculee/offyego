# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude Code per-developer settings (team settings.json is committed)
.claude/settings.local.json

# Agent diagnostic scripts (kept local, not tracked)
/diagnose-*.mjs
/diagnose-*.cjs
/capture-*.mjs
/capture-*.cjs
/verify-*.mjs
/verify-*.cjs
/verify-screenshots*/

# Raw GeoJSON source data used to generate the ROI polygon.
# The derived polygon lives in src/lib/ireland-polygon.ts which is committed.
# To regenerate: download Natural Earth ne_10m_admin_0_countries.geojson,
# place at data/, then run node scripts/extract-ireland-polygon.mjs
/data/

# Polygon extraction script (one-off tooling, not production code)
/scripts/extract-ireland-polygon.mjs

# Binary documents (receipts, PDFs, screenshots)
*.pdf

.vercel
.env*
